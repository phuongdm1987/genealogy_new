language: php

php:
  - 7.1

services:
  - redis-server

before_script:
  - composer self-update
  - composer update -o
  - chmod -R 777 storage
  - chmod -R 777 bootstrap/cache
  - php artisan migrate --env=testing

script:
  - ./vendor/bin/phpunit
