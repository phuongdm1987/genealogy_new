language: php

php:
  - 7.1

before_script:
  - composer self-update
  - composer update -o
  - chmod -R 777 storage
  - chmod -R 777 bootstrap
  - php artisan migrate --env=testing

script:
  - ./vendor/bin/phpunit
